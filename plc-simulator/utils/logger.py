"""Пустой файл-заглушка (не используется). Логирование настраивается напрямую в
точках входа через logging.basicConfig (simulator.py, sim_records.py), отдельной
обёртки нет. Нигде не импортируется."""
